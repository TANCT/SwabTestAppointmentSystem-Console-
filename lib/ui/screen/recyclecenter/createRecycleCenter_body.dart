import 'package:flutter/material.dart';

class CreateRecycleCenterBody extends StatelessWidget {
    final CreateReState _state;
  const CreateRecycleCenterBody({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}