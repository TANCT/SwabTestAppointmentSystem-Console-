import 'package:flutter/material.dart';

class RecycleCenterOnMap extends StatelessWidget {
  static Route route() => MaterialPageRoute(builder: (_) => RecycleCenterOnMap());
  const RecycleCenterOnMap({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}